class Solution:
    def sumZero(self, n: int) -> List[int]:
        arr = list()
        s = int()
        for i in range(1, n):
            arr.append(i)
            s += i
        arr.append(-s)
        return arr
        
